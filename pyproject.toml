[project]
name = "coh1t"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fonttools",
]

[dependency-groups]
dev = [
    "ruff",
    "pyright",
]

[tool.ruff]
line-length = 100

[tool.pyright]
typeCheckingMode = "basic"
